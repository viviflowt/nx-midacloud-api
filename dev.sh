#!/bin/bash

# rm -rf dist 2>/dev/null

# FILE='src/installments/lib/contracts.ts'

# ts-node -r tsconfig-paths/register $FILE

# npm run dev

# echo "nginx Whoami"
# curl -sL http://localhost/whoami
# ech

# echo "nginx Health"
# curl -sL http://localhost:3000/health
# echo

# echo "api Health"
# curl -sL http://localhost/api/health

# curl -sL http://midacloud.ddns.net/health

# Portainer
# https://localhost:9443

# for i in {1..100}; do
#   echo "Welcome $i times"
#   # curl -sL http://localhost/api/events/send
#   http POST http://midacloud/api/auth/forgot-password --json email=dev@dev --ignore-stdin
# done

# curl -sL http://localhost/api/health

# npx nx generate @nx/nest:library --name=common --buildable=true --no-interactive
# npx nx generate @nx/nest:library --name=core --buildable=true --no-interactive
# npx nx generate @nx/nest:library --name=repository --buildable=true --no-interactive
# npx nx generate @nx/nest:library --name=domain --buildable=true --no-interactive
# npx nx generate @nx/nest:library --name=provider --buildable=true --no-interactive

# npx nx generate @nx/node:setup-docker --project=gateway --no-interactive
# npx nx generate @nx/node:setup-docker --project=service-account --no-interactive
# npx nx generate @nx/node:setup-docker --project=service-access --no-interactive
# npx nx generate @nx/node:setup-docker --project=service-message --no-interactive
# npx nx generate @nx/node:setup-docker --project=service-tenent --no-interactive

# nc -v localhost 4222
