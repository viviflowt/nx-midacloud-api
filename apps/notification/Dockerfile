# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build notification`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t notification`.
FROM docker.io/node:18-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system notification && \
          adduser --system -G notification notification

COPY dist/apps/notification notification
RUN chown -R notification:notification .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix notification --omit=dev -f install

CMD [ "node", "notification" ]
